apiVersion: v1
kind: Service
metadata:
  name: office-portal
  namespace: eurooffice-shared
  labels:
    app.kubernetes.io/name: office-portal
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: office-portal
  ports:
    - name: http
      port: 80
      targetPort: http
